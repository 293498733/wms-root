# 功能需求

<!--
填写说明：
1. 每个章节都必须填写，不确定的地方标为「待确认」
2. 涉及接口时，必须提供完整的请求/响应字段表和 JSON 示例
3. 业务流程图可以用文字描述，但必须覆盖主流程和异常流程
4. 验收标准必须可量化、可测试
-->

## 功能名称

<!-- 格式：模块.子功能，一句话说清楚做什么 -->
<!-- 示例：返修通知单.核对与自动完成 -->

## 业务背景

<!-- 回答以下问题：
- 当前业务场景是什么？（谁在什么时候做什么操作）
- 现在有什么痛点？（人工操作？流程断裂？数据不一致？）
- 不做会有什么后果？
-->

## 功能要求

### 涉及的现有模块

<!-- 列出已知的相关模块、表、页面。不确定写「待确认」。 -->

### 核心业务流程

<!-- 按步骤描述主流程，例如：
1. 用户点击「开始处理」
2. 系统展示核对弹窗（按规格型号汇总明细）
3. 用户核对实物，点击「通过」/「退回」
4. 通过后系统自动创建入库单
...

同时列出异常流程：
- 重复点击处理 → 已处理状态不展示按钮
- 已作废单据 → 不可操作
-->

### 接口定义

<!-- 每个接口单独列出 -->

#### 接口 1：（名称）

- **路径**：`POST /xxx/xxx`
- **请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
|      |      | Y/N  |      |

- **请求示例**：
```json
{}
```

- **响应示例**（成功）：
```json
{}
```

- **响应示例**（失败）：
```json
{}
```

<!-- 如有多接口，复制上面的模板块 -->

### 前端变更（如有）

<!-- 涉及的页面、组件、交互变更 -->

## 数据库变更（如有）

<!-- 新增表 / 新增字段 / 修改索引等。不确定写「待确认」。 -->

## 限制条件

<!-- 每一条都是硬约束 -->

1. 不允许修改已有接口路径，除非明确说明。
2. 不允许删除已有业务逻辑。
3. 不允许全局重构。
4. 涉及数据库变更时，必须先输出 SQL、回滚 SQL 和影响范围。
5. 实现前必须先输出工程方案。

<!-- 可选：按项目增减 -->
<!-- 6. 必须兼容 XX 版本以上 -->
<!-- 7. 接口响应时间 < XXms -->
<!-- 8. 必须支持灰度发布 -->

## 验收标准

<!-- 必须可量化、可测试。以下为格式示例： -->

1. [接口] 调用 XX 接口，输入 YY，返回 ZZ。
2. [流程] 用户操作 A → 系统执行 B → 结果 C。
3. [数据] XX 表 YY 字段更新为 ZZ。
4. [回归] 不影响已有 XX 功能。

## 补充信息

<!-- 可选：
- 相关文档链接
- 原型图/设计稿路径
- 第三方系统对接说明
- 历史相关需求编号
- 已知边界情况
-->
